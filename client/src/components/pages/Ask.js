import React, { Component } from "react";
import { Router } from "@reach/router";

const Ask = (props) => {
  return (
    <>
      <div>there will be questions here i promise</div>
    </>
  );
};

export default Ask;
